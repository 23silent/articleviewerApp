import { connect } from "react-redux";
import FullArticleComponent from "../Components/FullArticle/FullArticleComponent";

const mapStateToProps = (state) => {
  return {
  };
};

const mapDispatchToProps = (dispatch) => {
  return {
  };
};

export default connect(mapStateToProps, mapDispatchToProps)(FullArticleComponent);
